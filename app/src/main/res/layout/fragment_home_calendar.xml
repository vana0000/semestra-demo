<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/two_tone_dark">

    <!-- Simple Header for Month Title and Navigation -->
    <LinearLayout
        android:id="@+id/layoutHomeTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="16dp"
        android:paddingTop="14dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/textHomeTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/nav_home"
            android:textColor="@color/two_tone_on_dark"
            android:textSize="22sp"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonPrevMonth"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="0dp"
                android:text="@string/month_prev"
                android:textAllCaps="false"
                android:textColor="@color/two_tone_on_dark_secondary"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/textCurrentMonth"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:textColor="@color/two_tone_on_dark"
                android:textSize="14sp"
                android:textStyle="bold" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonNextMonth"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="0dp"
                android:text="@string/month_next"
                android:textAllCaps="false"
                android:textColor="@color/two_tone_on_dark_secondary"
                android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Month Calendar View -->
    <com.kizitonwose.calendar.view.CalendarView
        android:id="@+id/monthCalendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="8dp"
        app:cv_dayViewResource="@layout/item_calendar_day"
        app:layout_constraintTop_toBottomOf="@id/layoutHomeTitle" />

    <!-- Unified Timeline Section -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerTimeline"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_bottom_sheet_surface"
        android:clipToPadding="false"
        android:elevation="4dp"
        android:paddingHorizontal="14dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/monthCalendarView" />

    <TextView
        android:id="@+id/textHomeEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@string/no_events_scheduled"
        android:textColor="@color/notion_text_secondary"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/recyclerTimeline"
        app:layout_constraintTop_toTopOf="@id/recyclerTimeline" />

</androidx.constraintlayout.widget.ConstraintLayout>
